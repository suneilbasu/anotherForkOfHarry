<h1>Edit</h1>
<%= form_for @blog do |h| %>
  <%= h.text_area :bodyText %>
  <%= h.select :flag, [['unread',0],['under review', 1], ['rejected', 2], ['redraft', 3], ['published', 4],] %>
  <%= h.submit "update" %>
<% end %>
