<h1>Edit</h1>
<%= form_for @blog do |h| %>
  <%= h.text_area :bodyText %>
  <%= h.select :flag, [['unread'],['under review','underreview'], ['rejected'], ['redraft'], ['published'],] %>
  <%= h.submit "update" %>
<% end %>
